# Global Ignores
# Should always include these ignores to prevent file cruft.
.DS_Store


# Eventually, SQL Dumps will be created/deleted via Grunt and should not
#  be archived via Git.
# Ignore .sql dumps
redesign/sql-dumps/*.sql

# Never move PStorm Configurations into Deployment:
.idea/

# Remove Drupal VM Files
# The only file changes for Drupal VM should be the config.yml file
#   as the drush.make file will be imported via SymLinks or copy/paste.
#
drupal-vm/*
**/local-settings.php
development/*

# Ignoring /files/ directory, which is for local project files
redesign/files/
temp/

# Ignoring /var/tmp directory, which will be generated by source scripts
/var/tmp
